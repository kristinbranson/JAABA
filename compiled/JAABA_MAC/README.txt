After creating the compiled versions using the build.m or *.prj files run the ./initCompile*.sh scripts to create executables that will be able to write log files and have access to the embedded files.

The paths in the prj files might need adjusting.
